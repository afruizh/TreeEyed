# Documentation

This documentatoin was created using sphinx for readthedocs.

Requirements:
```
pip install sphinx
pip install sphinx_rtd_theme
```

To build:

```
make html
```

Sometimes it is necessary to clean so all objects are built correctly, use:

```
make clean
```
